services:
  peanut:
    image: brandawg93/peanut:${IMAGE_VERSION:-latest}
    container_name: peanut
    hostname: peanut
    restart: unless-stopped
    volumes:
      - ./config:/config
#    ports:
#      - 8080:8080
    networks:
      - proxy
    env_file:
      - .env
    healthcheck:
      test: ["CMD-SHELL", "node healthcheck.mjs || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    # Use image HEALTHCHECK: `node healthcheck.mjs` → GET /api/ping (no wget in image; hard-coded :8080 wget fails if WEB_PORT differs).

networks:
  proxy:
    name: nginx-proxy-net
    external: true
